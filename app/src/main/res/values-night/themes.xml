<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ChAuth" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_purple_grey_color_2</item>
        <!-- Customize your theme here. -->
        <!-- Default font family -->
        <item name="android:fontFamily">@font/bebas_neue</item>
        <item name="android:textColor">@color/dark_grey</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>